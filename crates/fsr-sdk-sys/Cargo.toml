# SPDX-License-Identifier: MPL-2.0

[package]
name = "fsr-sdk-sys"
version = "0.1.0"
description = "Selected raw Rust bindings and explicit DX12 runtime loading for AMD FidelityFX SDK v2.3.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

[features]
default = []
dx12 = []
vulkan = []

[dependencies]
libloading = "0.9.0"
