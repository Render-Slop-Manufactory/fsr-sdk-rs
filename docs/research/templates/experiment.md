# Experiment: <question>

Copy to `records/YYYY-MM-DD-exp-description.md`. Replace placeholders and remove
these instructions. Record paths are relative to `docs/research/`.

- Investigation date:
- Topic, hypothesis, and exclusions:
- Observable success and failure conditions:

## Setup

Record repository revision and relevant uncommitted changes, pinned SDK/runtime,
toolchain, OS, and GPU/driver when relevant. Identify probe code and exact inputs.
Sanitize personal paths and secrets without losing reproduction information.

## Reproduction

Provide exact commands, prerequisites, and necessary artefact links.

## Observations

Record outcomes, exit statuses, and failures or inconclusive results. Distinguish
observed behavior from explanations. Identify which path actually ran: host
build, native loading, query, or GPU dispatch.

## Baseline delta and limits

What changed in our understanding? What does this experiment not establish?
Record reproduction limits and the next question, without declaring a design
accepted or implying broader hardware support.
